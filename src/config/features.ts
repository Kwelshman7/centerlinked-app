// Feature flags. Toggle community features (Feed, Messages, Posts) when ready.
// Phase 1: community is hidden in favour of the core Search + Program Sheet flow.
export const FEATURES = {
  community: false,
} as const;
